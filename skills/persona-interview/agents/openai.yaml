interface:
  display_name: "Persona Interview"
  short_description: "Create or revise a persona by interview"
  default_prompt: "Use $persona-interview to define a persona: derive a draft from this repository, question me one item at a time, dry-run it, then save it."
policy:
  allow_implicit_invocation: false
